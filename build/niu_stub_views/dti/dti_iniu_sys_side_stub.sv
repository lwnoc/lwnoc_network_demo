// Auto-generated by tools/gen_noc_niu_stubs.py.
// Review-only boundary stub with fully expanded vector widths.
// All outputs are tied to '0 so reviewers can focus on interface shape and bit width.
// Future extension: FUSA error related ports will be added on this boundary in a later revision.
// Requested source top module: dti_pr_iniu_async_sys_side
// Resolved source top module: dti_pr_iniu_async_sys_side
// Representative config: gpu_iniu_sys_config (TemplateIPConfig name=gpu_iniu_sys)
module dti_iniu_sys_side_stub (
    input  logic [0:0]   clk             ,  // clock input
    input  logic [0:0]   rst_n           ,  // active-low reset
    input  logic [0:0]   req_tvalid      ,
    input  logic [79:0]  req_tdata       ,
    input  logic [9:0]   req_tkeep       ,
    input  logic [0:0]   req_tlast       ,
    input  logic [5:0]   req_ttid        ,
    output logic [0:0]   req_tready      ,
    output logic [0:0]   rsp_tvalid      ,
    output logic [79:0]  rsp_tdata       ,
    output logic [9:0]   rsp_tkeep       ,
    output logic [0:0]   rsp_tlast       ,
    output logic [5:0]   rsp_ttid        ,
    input  logic [0:0]   rsp_tready      ,
    input  logic [0:0]   req_twakeup     ,
    output logic [0:0]   rsp_twakeup     ,
    output logic [15:0]  req_wptr_async  ,  // async FIFO write pointer
    input  logic [15:0]  req_rptr_async  ,  // async FIFO read pointer (async)
    input  logic [15:0]  req_rptr_sync   ,  // async FIFO read pointer (sync)
    output logic [112:0] req_pld_sync    ,  // async FIFO payload sync
    input  logic [15:0]  rsp_wptr_async  ,  // async FIFO write pointer
    output logic [15:0]  rsp_rptr_async  ,  // async FIFO read pointer (async)
    output logic [15:0]  rsp_rptr_sync   ,  // async FIFO read pointer (sync)
    input  logic [112:0] rsp_pld_sync    ,  // async FIFO payload sync
    output logic [0:0]   rsp_afifo_sb_err,  // ECC error flag (single/double bit)
    output logic [0:0]   rsp_afifo_db_err,  // ECC error flag (single/double bit)
    input  logic [9:0]   timeout_val     ,  // timeout value
    input  logic [0:0]   preq            ,
    input  logic [1:0]   pstate          ,
    output logic [1:0]   pactive         ,
    output logic [0:0]   paccept         ,
    output logic [0:0]   pdeny           ,
    input  logic [12:0]  lp_hub_rx_req   ,  // low-power interface
    output logic [12:0]  lp_hub_tx_req     // low-power interface
);

    // Future boundary note:
    // FUSA error reporting ports are intentionally omitted in this review stub today,
    // but this boundary is expected to grow dedicated FUSA error signaling later.

    // Review-only stub behavior: tie every output low.
    assign req_tready = '0;
    assign rsp_tvalid = '0;
    assign rsp_tdata = '0;
    assign rsp_tkeep = '0;
    assign rsp_tlast = '0;
    assign rsp_ttid = '0;
    assign rsp_twakeup = '0;
    assign req_wptr_async = '0;
    assign req_pld_sync = '0;
    assign rsp_rptr_async = '0;
    assign rsp_rptr_sync = '0;
    assign rsp_afifo_sb_err = '0;
    assign rsp_afifo_db_err = '0;
    assign pactive = '0;
    assign paccept = '0;
    assign pdeny = '0;
    assign lp_hub_tx_req = '0;

endmodule
